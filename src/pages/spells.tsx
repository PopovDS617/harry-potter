import React from 'react';
import Layout from '../components/layout/Layout';

type Props = {};

const SpellsPage = (props: Props) => {
  return (
    <Layout>
      {' '}
      <div>SpellsPage</div>
    </Layout>
  );
};

export default SpellsPage;
